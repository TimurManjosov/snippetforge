# ============================================
# SERVER CONFIGURATION
# ============================================

# Server port
PORT=3001

# Node environment (development | production | test)
NODE_ENV=development

# ============================================
# DATABASE CONFIGURATION
# ============================================

# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://snippetforge:dev_password@localhost:5432/snippetforge

# ============================================
# JWT CONFIGURATION
# ============================================

# JWT secret key (MUST be at least 32 characters in production!)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-characters-long

# JWT token expiration
# Formats: "15m" (15 minutes), "1h" (1 hour), "7d" (7 days)
JWT_EXPIRES_IN=15m

# ============================================
# CORS CONFIGURATION
# ============================================

# Allowed origin for CORS (frontend URL)
CORS_ORIGIN=http://localhost:3000

# ============================================
# SWAGGER/API DOCUMENTATION
# ============================================

# Enable Swagger in production (default: false)
# ENABLE_SWAGGER=true

# API version (optional)
# API_VERSION=0.1.0

# ============================================
# OPTIONAL CONFIGURATION
# ============================================

# Log level (error | warn | log | debug | verbose)
# LOG_LEVEL=debug

# Rate limiting (future)
# RATE_LIMIT_TTL=60
# RATE_LIMIT_MAX=100
